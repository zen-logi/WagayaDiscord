services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "6120:6120"
    volumes:
      - ./data:/app/data
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://0.0.0.0:6120
    restart: unless-stopped
